# Repository Copilot Instructions

- After completing code changes, always run the necessary quality checks before sending the final response.
- For Dart or Flutter edits:
  - Run `dart format` on every changed Dart file.
  - Run `flutter analyze`.
  - If issues are related to the current changes, fix them and re-run checks.
- In the final response, summarize format and analyze results.
